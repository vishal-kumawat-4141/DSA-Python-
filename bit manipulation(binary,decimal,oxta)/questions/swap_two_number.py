# swapping two numbers using bit operators:--->
a = 10
b = 20
# a = a ^ b
# b = a ^ b   # (a^b)^b == a  because b ^ b = 0
# a = a ^ b   # (a^b)^a == b  because a ^ a = 0
print(f"a = {a} and b  = { b}")
